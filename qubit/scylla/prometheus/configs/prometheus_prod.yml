global:
  scrape_interval: 15s

  # Attach these labels to any time series or alerts when communicating with
  # external systems (federation, remote storage, Alertmanager).
  external_labels:
    monitor: 'scylla-prod'

scrape_configs:
- job_name: scylla
  honor_labels: true
  file_sd_configs:
    - files:
      - /etc/prometheus/scylla_servers_prod.yml

- job_name: node_exporter
  honor_labels: true
  file_sd_configs:
    - files:
      - /etc/prometheus/node_exporter_servers_prod.yml
