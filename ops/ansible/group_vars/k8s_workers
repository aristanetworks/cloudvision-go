certificates:
  worker.pem: "{{ lookup('file', inventory_dir+'/files/k8s/worker.pem') }}"
  worker-key.pem: "{{ lookup('file', inventory_dir+'/files/k8s/worker-key.pem') }}"
  ca.pem: "{{ lookup('file', inventory_dir+'/files/k8s/ca.pem') }}"

manifests:
  - kube-proxy-worker.yaml

kubeconfig: worker-kubeconfig
