---

# FIXME:
#  Once the PXE kickstart based install is broken down into granular ASB
#  stages we can use disk partitioning steps etc here. Until then, just
#  do PXE kexec to launch role specific kickstart based install.
#
- name: "Verify pxe server is reachable"
  command: "ping -c 5 pxe"

- name: "Update datacenter.servers with status CLEANUP"
  command: "{{ASB_DB_UPDATE_CLEANUP}}"

- name: "Starting PXE installation"
  shell: "pxe-kexec -n pxe <<< \"{{ROLE_NAME}}-pxe-kexec\""
...

