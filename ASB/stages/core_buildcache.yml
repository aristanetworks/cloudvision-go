---
- name: "Verify pxe server is reachable"
  command: "ping -c 5 pxe"

- name: "Update datacenter.servers with status CLEANUP"
  command: "{{ASB_DB_UPDATE_RESYNC_RUNNING}}"

# TODO: This shouldn't be "centos7" hardcoded but rather based on role
- name: "Starting PXE installation"
  shell: "pxe-kexec -n pxe <<< \"centos7\""
...

